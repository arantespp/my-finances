{
  "name": "root",
  "private": false,
  "scripts": {
    "deploy:hosting": "npm run deploy --prefix client",
    "format:fix": "pretty-quick --staged",
    "precommit": "run-s format:fix",
    "format:check": "prettier --config ./.prettierrc --list-different \"**/*{.ts,.js,.json,.css,.scss}\""
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "npm-run-all": "^4.1.3",
    "prettier": "1.14.2",
    "pretty-quick": "^1.6.0"
  }
}
